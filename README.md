# slides.jimmac.eu

Browser-based viewer for [Podium](https://gitlab.gnome.org/feaneron/podium) presentations.
Drop a `.podium` file to render slides with custom stylesheets, media assets, and
the title-and-list layout — no build step needed.
